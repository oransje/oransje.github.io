---
layout: layout.njk
title: Ordinary Life
---

{% assign posts = collections.ordinaryLife | reverse %}
{% for post in posts %}
  <li><a href="{{ post.url }}">{{ post.data.title }}</a> <i>(written {{ post.date | date: "%b %d, %Y"}})</i></li>
{% endfor %}